class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        re1 = ''.join(sorted(s))
        re2 = ''.join(sorted(t))
        if(re1==re2):
            return True
        else:
            return False
